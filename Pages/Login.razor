@page "/login"
@implements IAsyncDisposable
@using ShopOwnerSimulator.State
@using ShopOwnerSimulator.Services
@using Microsoft.JSInterop
@using System.Text.Json
@using Microsoft.AspNetCore.Components.Web
@inject GameState GameState
@inject IPlayFabService PlayFabService
@inject NavigationManager Navigation
@inject IJSRuntime JSRuntime

<PageTitle>로그인 - Shop Owner Simulator</PageTitle>

<div class="login-container">
    <div class="login-box">
        <div class="login-header">
            <h1>🏪 상점주인 시뮬레이터</h1>
            <p class="tagline">당신만의 거래 제국을 건설하세요</p>
        </div>

        @if (IsGuestMode)
        {
            <div class="guest-mode">
                <div class="mode-switch">
                    <button class="mode-btn" @onclick="() => IsGuestMode = false">계정 로그인</button>
                </div>
                
                <div class="guest-info">
                    <h3>🎮 게스트로 플레이</h3>
                    <p>로그인 없이 바로 시작하세요. 데이터는 이 기기에 저장됩니다.</p>
                    <p class="warning">⚠️ 브라우저 데이터 삭제 시 진행 상황이 사라집니다.</p>
                </div>

                <button class="btn btn-guest" @onclick="LoginAsGuest" disabled="@IsLoading">
                    @(IsLoading ? "시작 중..." : "게스트로 시작하기")
                </button>
            </div>
        }
        else
        {
            <div class="account-mode">
                <div class="mode-switch">
                    <button class="mode-btn" @onclick="() => IsGuestMode = true">게스트로 플레이</button>
                </div>

                <div class="login-tabs">
                    <button class="tab @(IsRegisterMode ? "" : "active")" @onclick="() => IsRegisterMode = false">
                        로그인
                    </button>
                    <button class="tab @(IsRegisterMode ? "active" : "")" @onclick="() => IsRegisterMode = true">
                        회원가입
                    </button>
                </div>

                <div class="login-form">
                    @if (IsRegisterMode)
                    {
                        <div class="form-group">
                            <label>사용자명</label>
                            <input type="text" @bind="Username" class="form-control" placeholder="닉네임 입력">
                        </div>
                    }

                    <div class="form-group">
                        <label>이메일</label>
                        <input type="email" @bind="Email" class="form-control" placeholder="이메일 입력">
                    </div>

                    <div class="form-group">
                        <label>비밀번호</label>
                        <input type="password" @bind="Password" class="form-control" placeholder="비밀번호 입력" 
                            @onkeyup="OnPasswordKeyUp">
                    </div>

                    @if (IsRegisterMode)
                    {
                        <div class="form-group">
                            <label>비밀번호 확인</label>
                            <input type="password" @bind="PasswordConfirm" class="form-control" placeholder="비밀번호 재입력">
                        </div>
                    }

                    <button class="btn btn-login" @onclick="LoginOrRegister" disabled="@IsLoading">
                        @(IsLoading ? "처리 중..." : IsRegisterMode ? "회원가입" : "로그인")
                    </button>

                    @if (!string.IsNullOrEmpty(ErrorMessage))
                    {
                        <div class="error-message">
                            @ErrorMessage
                        </div>
                    }
                </div>
            </div>
        }

        <div class="login-features">
            <h3>무엇을 할 수 있나요?</h3>
            <ul class="feature-list">
                <li>⛏️ 던전을 탐험하고 용병을 관리하세요</li>
                <li>🔨 강력한 장비를 제작하세요</li>
                <li>💼 개인 상점을 운영하세요</li>
                <li>📈 플레이어 거래소에서 거래하세요</li>
                <li>⚔️ 팀을 구축하고 강화하세요</li>
                <li>💰 부와 권력을 쌓으세요</li>
            </ul>
        </div>

        <div class="login-footer">
            <p class="version">Version 1.0.0</p>
            <p class="privacy">PlayFab을 통해 안전하게 데이터가 저장됩니다</p>
        </div>
    </div>

    <div class="background-animation">
        <div class="float-item item-1">⛏️</div>
        <div class="float-item item-2">💼</div>
        <div class="float-item item-3">🔨</div>
        <div class="float-item item-4">📈</div>
        <div class="float-item item-5">⚔️</div>
        <div class="float-item item-6">💰</div>
    </div>
</div>

<style>
    .login-container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        position: relative;
        overflow: hidden;
    }

    .background-animation {
        position: absolute;
        width: 100%;
        height: 100%;
        z-index: 1;
        pointer-events: none;
    }

    .float-item {
        position: absolute;
        font-size: 4rem;
        opacity: 0.1;
        animation: float infinite ease-in-out;
    }

    .float-item.item-1 { top: 10%; left: 10%; animation-duration: 6s; }
    .float-item.item-2 { top: 20%; right: 15%; animation-duration: 7s; animation-delay: 1s; }
    .float-item.item-3 { bottom: 20%; left: 20%; animation-duration: 8s; animation-delay: 2s; }
    .float-item.item-4 { top: 30%; right: 5%; animation-duration: 9s; animation-delay: 0.5s; }
    .float-item.item-5 { bottom: 30%; right: 20%; animation-duration: 10s; animation-delay: 1.5s; }
    .float-item.item-6 { top: 50%; left: 5%; animation-duration: 7.5s; animation-delay: 2.5s; }

    @@keyframes float {
        0%, 100% { transform: translateY(0px) translateX(0px); }
        50% { transform: translateY(-30px) translateX(20px); }
    }

    .login-box {
        background: white;
        border-radius: 1rem;
        padding: 3rem;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        max-width: 450px;
        width: 100%;
        z-index: 10;
        position: relative;
        max-height: 90vh;
        overflow-y: auto;
    }

    .login-header {
        text-align: center;
        margin-bottom: 2rem;
    }

    .login-header h1 {
        margin: 0 0 0.5rem 0;
        font-size: 1.8rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .tagline {
        margin: 0;
        color: #666;
        font-size: 0.95rem;
    }

    .mode-switch {
        text-align: center;
        margin-bottom: 1.5rem;
    }

    .mode-btn {
        background: #f8f9fa;
        border: 1px solid #ddd;
        padding: 0.5rem 1.5rem;
        border-radius: 2rem;
        cursor: pointer;
        font-size: 0.875rem;
        color: #666;
        transition: all 0.2s;
    }

    .mode-btn:hover {
        background: #e9ecef;
        color: #333;
    }

    .guest-info {
        background: #e7f3ff;
        border: 1px solid #b3d9ff;
        border-radius: 0.5rem;
        padding: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .guest-info h3 {
        margin: 0 0 0.75rem 0;
        font-size: 1rem;
        color: #0056b3;
    }

    .guest-info p {
        margin: 0 0 0.5rem 0;
        font-size: 0.875rem;
        color: #666;
        line-height: 1.5;
    }

    .guest-info .warning {
        color: #856404;
        font-weight: 600;
    }

    .login-tabs {
        display: flex;
        gap: 0;
        margin-bottom: 2rem;
        border-bottom: 2px solid #eee;
    }

    .tab {
        flex: 1;
        padding: 0.75rem;
        background: none;
        border: none;
        border-bottom: 3px solid transparent;
        cursor: pointer;
        font-weight: 600;
        color: #999;
        transition: all 0.2s;
    }

    .tab.active {
        color: #667eea;
        border-bottom-color: #667eea;
    }

    .login-form {
        margin-bottom: 2rem;
    }

    .form-group {
        margin-bottom: 1.5rem;
    }

    .form-group label {
        display: block;
        font-weight: 600;
        margin-bottom: 0.5rem;
        color: #333;
        font-size: 0.95rem;
    }

    .form-control {
        width: 100%;
        padding: 0.75rem 1rem;
        border: 1px solid #ddd;
        border-radius: 0.5rem;
        font-size: 0.95rem;
        transition: all 0.2s;
        font-family: inherit;
    }

    .form-control:focus {
        outline: none;
        border-color: #667eea;
        box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .btn {
        width: 100%;
        padding: 1rem;
        color: white;
        border: none;
        border-radius: 0.5rem;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        transition: all 0.3s;
    }

    .btn-login {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .btn-guest {
        background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
    }

    .btn:hover:not(:disabled) {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
    }

    .btn:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }

    .error-message {
        margin-top: 1rem;
        padding: 1rem;
        background: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
        border-radius: 0.5rem;
        font-size: 0.875rem;
    }

    .login-features {
        background: #f8f9fa;
        border-radius: 0.5rem;
        padding: 1.5rem;
        margin-bottom: 2rem;
    }

    .login-features h3 {
        margin: 0 0 1rem 0;
        font-size: 1rem;
        color: #333;
    }

    .feature-list {
        margin: 0;
        padding-left: 0;
        list-style: none;
    }

    .feature-list li {
        padding: 0.5rem 0;
        color: #666;
        font-size: 0.9rem;
    }

    .login-footer {
        text-align: center;
        border-top: 1px solid #eee;
        padding-top: 1rem;
    }

    .version, .privacy {
        margin: 0.25rem 0;
        font-size: 0.75rem;
        color: #999;
    }

    @@media (max-width: 600px) {
        .login-box {
            margin: 1rem;
            padding: 2rem 1.5rem;
        }
        .login-header h1 {
            font-size: 1.5rem;
        }
        .float-item {
            font-size: 3rem;
        }
    }
</style>

@code {
    private bool IsGuestMode = true;
    private bool IsRegisterMode = false;
    private string Username = "";
    private string Email = "";
    private string Password = "";
    private string PasswordConfirm = "";
    private bool IsLoading = false;
    private string ErrorMessage = "";

    protected override async Task OnInitializedAsync()
    {
        if (GameState.IsInitialized)
        {
            Navigation.NavigateTo("/");
        }
    }

    private async Task LoginAsGuest()
    {
        IsLoading = true;
        ErrorMessage = "";

        try
        {
            var deviceId = GetOrCreateDeviceId();
            // Try server-side guest login first. If it fails, fall back to local guest.
            try
            {
                var playFabId = await PlayFabService.LoginAsGuestAsync(deviceId);
                // Initialize game using server player id so data is synced
                await GameState.InitializeWithPlayerIdAsync(playFabId);
                Navigation.NavigateTo("/");
                return;
            }
            catch (Exception ex)
            {
                    // Log full details (may include PlayFab API JSON payload in InnerException)
                    Console.Error.WriteLine($"Guest server login failed, falling back to local guest: {ex}");

                    // If PlayFab returned a known error, show a friendly message in UI
                    var inner = ex.InnerException?.Message ?? ex.Message;
                    if (!string.IsNullOrEmpty(inner) && inner.Contains("PlayerCreationDisabled", StringComparison.OrdinalIgnoreCase))
                    {
                        ErrorMessage = "서버에서 새 플레이어 생성을 허용하지 않아 서버 게스트 로그인이 불가합니다. 로컬 게스트로 계속합니다.";
                    }
                    else if (!string.IsNullOrEmpty(inner) && inner.Contains("Player creations have been disabled", StringComparison.OrdinalIgnoreCase))
                    {
                        ErrorMessage = "서버에서 새 플레이어 생성을 허용하지 않아 서버 게스트 로그인이 불가합니다. 로컬 게스트로 계속합니다.";
                    }
                    else
                    {
                        // Generic user-friendly hint
                        ErrorMessage = "서버 게스트 로그인이 실패하여 로컬 게스트로 시작합니다.";
                    }
            }

            // Fallback: Create a local, ephemeral guest player (no server calls).
            var guestPlayer = new ShopOwnerSimulator.Models.Entities.Player
            {
                Id = $"guest_{deviceId}",
                Username = "게스트",
                Level = 1,
                Experience = 0,
                Gold = 1000,
                Crystal = 0,
                CreatedAt = DateTime.UtcNow,
                LastLoginAt = DateTime.UtcNow
            };

            await GameState.InitializeGuestAsync(guestPlayer);
            Navigation.NavigateTo("/");
        }
        catch (Exception ex)
        {
            ErrorMessage = $"게스트 로그인 실패: {ex.Message}";
        }
        finally
        {
            IsLoading = false;
        }
    }

    private async Task LoginOrRegister()
    {
        if (string.IsNullOrWhiteSpace(Email) || string.IsNullOrWhiteSpace(Password))
        {
            ErrorMessage = "이메일과 비밀번호를 입력하세요";
            return;
        }

        if (IsRegisterMode)
        {
            if (string.IsNullOrWhiteSpace(Username))
            {
                ErrorMessage = "사용자명을 입력하세요";
                return;
            }

            if (Password != PasswordConfirm)
            {
                ErrorMessage = "비밀번호가 일치하지 않습니다";
                return;
            }

            await Register();
        }
        else
        {
            await PerformLoginAsync();
        }
    }

    private async Task PerformLoginAsync()
    {
        IsLoading = true;
        ErrorMessage = "";

        try
        {
            var playerId = await PlayFabService.LoginWithEmailAsync(Email, Password);
            // Initialize using server player id so we load server state
            await GameState.InitializeWithPlayerIdAsync(playerId);
            Navigation.NavigateTo("/");
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Login error: {ex}");
            ErrorMessage = $"로그인 실패: 이메일 또는 비밀번호를 확인하세요";
        }
        finally
        {
            IsLoading = false;
        }
    }

    private async Task Register()
    {
        IsLoading = true;
        ErrorMessage = "";

        try
        {
            // Generate an internal ASCII-only username to avoid PlayFab username/displayName charset and reuse issues
            var internalUsername = $"user_{Guid.NewGuid().ToString("N").Substring(0,8)}";

            var playerId = await PlayFabService.RegisterWithEmailAsync(Email, Password, internalUsername);
            // Set the display name to the user's requested Username (may contain Hangul)
            try
            {
                if (!string.IsNullOrWhiteSpace(Username))
                {
                    await PlayFabService.UpdateDisplayNameAsync(Username);
                }
            }
            catch (Exception dnEx)
            {
                Console.Error.WriteLine($"Update display name failed: {dnEx}");
                // Non-fatal: continue even if display name update fails
            }

            // If registration succeeded, initialize with server player id
            await GameState.InitializeWithPlayerIdAsync(playerId);
            Navigation.NavigateTo("/");
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Register error: {ex}");

            // Prefer the most detailed JSON payload from PlayFab. Try inner exception first, then outer.
            var payload = ex.Message;
            var jsonCandidate = ex.InnerException?.Message ?? ex.Message;

            if (!string.IsNullOrEmpty(jsonCandidate) && jsonCandidate.TrimStart().StartsWith("{"))
            {
                try
                {
                    using var doc = JsonDocument.Parse(jsonCandidate);

                    // PlayFab may include an 'error' or 'errorMessage' field, and 'errorDetails' with field-specific messages
                    if (doc.RootElement.TryGetProperty("errorDetails", out var ed) && ed.ValueKind == JsonValueKind.Object)
                    {
                        if (ed.TryGetProperty("Username", out var u) && u.ValueKind == JsonValueKind.Array && u.GetArrayLength() > 0)
                        {
                            payload = u[0].GetString() ?? "사용자 이름을 확인하세요";
                        }
                    }

                    if (string.IsNullOrEmpty(payload) && doc.RootElement.TryGetProperty("errorMessage", out var em))
                    {
                        payload = em.GetString() ?? payload;
                    }

                    if (string.IsNullOrEmpty(payload) && doc.RootElement.TryGetProperty("error", out var e))
                    {
                        var err = e.GetString();
                        if (string.Equals(err, "UsernameNotAvailable", StringComparison.OrdinalIgnoreCase))
                        {
                            payload = "사용자 이름이 이미 사용 중입니다.";
                            // For username conflicts, suggest slight modification? handled by UI later
                        }
                        else
                        {
                            payload = err;
                        }
                    }
                }
                catch
                {
                    // If parsing fails, keep outer message
                }
            }

            if (string.IsNullOrEmpty(payload)) payload = "회원가입 실패: 알 수 없는 오류가 발생했습니다.";

            // If PlayFab disallows player creation, fall back to a local account so user can still play
            if (jsonCandidate != null && (jsonCandidate.Contains("PlayerCreationDisabled") || jsonCandidate.Contains("Player creations have been disabled")))
            {
                // Create a local player with requested username
                var deviceId = GetOrCreateDeviceId();
                var localPlayer = new ShopOwnerSimulator.Models.Entities.Player
                {
                    Id = $"local_{deviceId}",
                    Username = Username ?? "로컬플레이어",
                    Level = 1,
                    Experience = 0,
                    Gold = 1000,
                    Crystal = 0,
                    CreatedAt = DateTime.UtcNow,
                    LastLoginAt = DateTime.UtcNow
                };

                await GameState.InitializeGuestAsync(localPlayer);
                ErrorMessage = "서버에서 신규 계정 생성을 허용하지 않아 로컬 계정으로 로그인했습니다.";
                Navigation.NavigateTo("/");
                IsLoading = false;
                return;
            }
            // If the failure was a username conflict, try automatic alternative names a few times
            var lowered = jsonCandidate?.ToLowerInvariant() ?? string.Empty;
            if (lowered.Contains("user name already exists") || payload.Contains("사용자 이름") && payload.Contains("중"))
            {
                const int maxAttempts = 5;
                var baseName = string.IsNullOrWhiteSpace(Username) ? "player" : Username;
                for (int i = 0; i < maxAttempts; i++)
                {
                    var alt = $"{baseName}_{RandomNumber(1000, 9999)}";
                    ErrorMessage = $"사용자명 '{Username}'은(는) 사용 중입니다. '{alt}'으로 재시도({i+1}/{maxAttempts})...";
                    try
                    {
                        var playerId = await PlayFabService.RegisterWithEmailAsync(Email, Password, alt);
                        await GameState.InitializeWithPlayerIdAsync(playerId);
                        Navigation.NavigateTo("/");
                        IsLoading = false;
                        return;
                    }
                    catch (Exception retryEx)
                    {
                        Console.Error.WriteLine($"Retry register with '{alt}' failed: {retryEx}");
                        // if it's a different error (like PlayerCreationDisabled), break and handle below
                        var rcandidate = retryEx.InnerException?.Message ?? retryEx.Message;
                        if (!string.IsNullOrEmpty(rcandidate) && (rcandidate.Contains("PlayerCreationDisabled") || rcandidate.Contains("Player creations have been disabled")))
                        {
                            break;
                        }
                        // otherwise continue to next alt
                    }
                }

                // If all retries failed, set message for user
                ErrorMessage = "사용자 이름 충돌로 자동 생성 시도 후에도 실패했습니다. 다른 이름을 입력해 주세요.";
                IsLoading = false;
                return;
            }

            // Show the best available message to user
            ErrorMessage = payload.StartsWith("회원가입 실패") ? payload : $"회원가입 실패: {payload}";
        }
    }

    private static int RandomNumber(int min, int max)
    {
        return new System.Random().Next(min, max);
    }

    private string GetOrCreateDeviceId()
    {
        try
        {
            var deviceId = JSRuntime.InvokeAsync<string>("ShopOwnerSimulator.getLocalStorage", "deviceId").Result;
            if (string.IsNullOrEmpty(deviceId))
            {
                deviceId = Guid.NewGuid().ToString();
                JSRuntime.InvokeVoidAsync("ShopOwnerSimulator.setLocalStorage", "deviceId", deviceId);
            }
            return deviceId;
        }
        catch
        {
            return Guid.NewGuid().ToString();
        }
    }

    private void OnPasswordKeyUp(KeyboardEventArgs e)
    {
        if (e?.Key == "Enter")
        {
            _ = LoginOrRegister();
        }
    }

    async ValueTask IAsyncDisposable.DisposeAsync() { }
}